import { css } from 'lit';

/** Keyboard shortcut hint — e.g. <span class="tool-shortcut-hint">(⌘G)</span> */
export const toolShortcutHintStyle = css`
  .tool-shortcut-hint {
    opacity: 0.5;
    font-size: 0.8em;
  }
`;
